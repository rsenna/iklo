# `spec/tasks/` — active epic plan & todo list

This directory holds the **currently active** epic's implementation plan and
task list. Only one epic is in flight at a time.

## Layout

```
spec/tasks/
  README.md   → this file
  plan.md     → technical implementation plan for the active epic
  todo.md     → ordered task list with acceptance criteria
```

## Lifecycle

- Generated by `/plan` from a `spec/<epic>/SPEC.md`.
- Executed one task at a time by `/build` (RED → GREEN → regression → commit).
- When switching epics, `plan.md` and `todo.md` are **overwritten** in a
  commit whose subject contains `CLEANING_TASKS` (see
  [`../SPEC.md` § Task lifecycle & `CLEANING_TASKS`](../SPEC.md#task-lifecycle--cleaning_tasks)).

## Recovering a previous plan

```bash
git log --grep=CLEANING_TASKS                    # find task-reset boundaries
git show <commit>^:spec/tasks/todo.md            # inspect the previous epic's todo
```

## Current epic

_None active._ The next epic will be `substrate` (see
[ADR-0001](../decisions/ADR-0001-substrate-boundary.md)); its
`spec/substrate/SPEC.md` has not been authored yet.
