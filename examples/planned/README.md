# `examples/planned/`

`.iklo` programs that **do not run today** but showcase syntax we intend to
support. They serve as executable design targets — when the corresponding
features land, the file moves up to [`../`](../) (i.e. becomes a runnable
example).

Do not assume anything here parses.
